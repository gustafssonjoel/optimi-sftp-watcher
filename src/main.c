#include <stdio.h>

#include "app_error.h"
#include "config.h"
#include "sftp_watcher.h"

int main(int argc, char **argv) {
    (void)argc;
    (void)argv;

    fputs("optimi-sftp-watcher scaffold only; implementation pending", stderr);
    return APP_ERR_NOT_IMPLEMENTED;
}
